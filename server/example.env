# General settings
CLOUD_INSTANCE=https://login.microsoftonline.com/ # cloud instance string should end with a trailing slash
TENANT_ID=xyz
CLIENT_ID=xyz
CLIENT_SECRET='xyz'

REDIRECT_URI=http://localhost:3000/auth/callback
POST_LOGOUT_REDIRECT_URI=/

GRAPH_API_ENDPOINT=https://graph.microsoft.com/ # graph api endpoint string should end with a trailing slash

EXPRESS_SESSION_SECRET=xyz

# Settings for msal-node
OAUTH_SCOPES='user.read,calendars.readwrite,mailboxsettings.read,teamsettings.readwrite.all,schedule.readwrite.all'
OAUTH_AUTHORITY=https://login.microsoftonline.com/xyz/